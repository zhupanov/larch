---

# larch-run-lifecycle: shared-v1 skill=fm
name: fm
description: "Use when forcing an implementation with Claude subagent implement + self-review and auto-merge. Shortcut for /implement --force --self-review --self-implement --merge (same as /f --merge)."
argument-hint: "<arguments>"
allowed-tools: Bash(*scripts/larch.sh:*), Skill, Read
---

**MANDATORY: Follow the complete shared lifecycle contract in `${CLAUDE_PLUGIN_ROOT}/skills/shared/run-lifecycle.md` with declared skill `fm`.**

**MANDATORY: READ ENTIRE FILE before composing user-facing prose: `${CLAUDE_PLUGIN_ROOT}/skills/shared/readability-style.md`.**

Auto-generated alias created by larch /alias. Invokes /implement with preset flags.

## Usage

/fm $ARGUMENTS is equivalent to /implement --force --self-review --self-implement --merge $ARGUMENTS (same as /f --merge $ARGUMENTS).

## Flags

All `/implement` **public** flags (`--merge`, `--forked`, `--draft`, `--no-admin-fallback`, `--no-logs-commit`, `--coder`, `--run-id`, `--force`, `-f`, `--self-review`, `--self-implement`) may appear in `$ARGUMENTS` after the preset flags and are forwarded verbatim **when supported**; removed argv surfaces (`--auto`, `--dynamic-archetypes`, `--no-dynamic-archetypes`, `--quick`, `--inline`, `--issue`, `--session-env`, verbal feature tails, etc.) MUST NOT be forwarded. The **positional** `<issue-N>` (digits only) is mandatory and is forwarded as the **sole** non-flag tail after all flags.

## Behavior

Invoke the Skill tool:
- Try skill: "implement" first (bare name). If no skill matches, try skill: "larch:implement" (fully-qualified plugin name).
- args: --lifecycle-parent-context "$CONTEXT_FILE" --force --self-review --self-implement --merge $ARGUMENTS

After the Skill tool loads /implement, the child skill MUST begin execution at its Step 0 (session setup). Do not investigate the codebase, plan, or implement anything before Step 0 completes.

Generated by larch /alias v52.11.0
